// Package auth implements vey's authentication flows: interactive
// password+TOTP login, transparent refresh-token rotation, and credential
// storage (OS keyring with a 0600-file fallback).
package auth
